version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20.x
    commands:
      - echo "Entering backend directory..."
      - cd backend
      - yarn install

  build:
    commands:
      - cd backend
      - yarn build

artifacts:
  base-directory: backend
  files:
    - "**/*"
  discard-paths: yes
